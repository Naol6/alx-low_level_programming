#include <stdio.h>
/**
 * main - number of argument passed on it
 * @argc: number of comand line
 * @argv: array
 * Return: the value
*/

int main(int argc, char *argv[])
{
printf("%d\n", argc - 1);
return (0);
}
